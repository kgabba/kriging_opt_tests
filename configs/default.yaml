n_min_points: 15

outer:
  n_trials: 30
  seed: 42
  n_startup_trials: 10
  n_lags_min: 10
  n_lags_max: 14
  max_dist_percentile_min: 40.0
  max_dist_percentile_max: 60.0
  tolerance_deg_min: 16.0
  tolerance_deg_max: 24.0
  radius_prior_scale_min: 1.0
  radius_prior_scale_max: 1.5
  min_pairs: 8

inner:
  n_trials: 200
  seed: 42
  n_startup_trials: null
  radius_margin: 0.20
  nugget_margin: 0.20
  range_scale_min: 0.80
  range_scale_max: 1.20
  nmax_min: 5
  nmax_max: 40

delete_d:
  holdout_size: null
  n_repeats: 25
  seed: 42

optuna:
  invalid_penalty: 1000000.0
  n_ei_candidates: 64
