n_min_points: 15

variogram_map:
  n_lags: 10
  max_dist_percentile: 50.0

moi:
  max_lag_fraction: 0.8
  eps: 1.0e-8

rose:
  step_deg: 15.0
  n_lags: 8
  max_dist_percentile: 50.0
  min_pairs: 5

directional:
  tolerance_deg: 25.0
  n_lags: 8
  max_dist_percentile: 50.0
  min_pairs: 5

search_space:
  radius_margin: 0.20
  nugget_margin: 0.20
  range_scale_min: 0.80
  range_scale_max: 1.20
  nmax_min: 5
  nmax_max: 30

delete_d:
  holdout_size: null
  n_repeats: 8
  seed: 42

optuna:
  n_trials: 20
  seed: 42
  n_startup_trials: 8
  n_ei_candidates: 32
  invalid_penalty: 1000000.0
