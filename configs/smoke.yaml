n_min_points: 15

outer:
  n_trials: 3
  seed: 42
  n_startup_trials: 2
  n_lags_min: 10
  n_lags_max: 12
  max_dist_percentile_min: 45.0
  max_dist_percentile_max: 55.0
  tolerance_deg_min: 18.0
  tolerance_deg_max: 22.0
  radius_prior_scale_min: 1.2
  radius_prior_scale_max: 1.5
  min_pairs: 5

inner:
  n_trials: 10
  seed: 42
  n_startup_trials: 4
  radius_margin: 0.20
  nugget_margin: 0.20
  range_scale_min: 0.80
  range_scale_max: 1.20
  nmax_min: 5
  nmax_max: 30

delete_d:
  holdout_size: null
  n_repeats: 8
  seed: 42

optuna:
  invalid_penalty: 1000000.0
  n_ei_candidates: 32
